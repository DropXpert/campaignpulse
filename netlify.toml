[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps=false"

[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888
  framework = "next"
